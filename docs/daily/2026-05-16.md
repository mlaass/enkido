# Daily Summary 2026-05-16

**Total commits:** 32 (excluding 1 daily-summary meta-commit and 1 merge commit)  
**Contributors:** Moritz Laass  
**Period:** 2026-05-15 ~11:52 UTC+2 → 2026-05-16 ~01:47 UTC+2

---

## cedar/

**Files touched:** ~53 | **Commits:** 8

- `feat: runtime MIDI event source` (dfd6d97) — Adds `MIDI_QUERY` opcode and `MidiQueueState` with SPSC ring so live MIDI events feed `POLY` through the same `OutputEvents` shape as `SEQPAT_QUERY`; `StatePool::resolve_output_events()` makes POLY source-agnostic. 12 files, 8 new Catch2 cases.
- `feat: time-based interpolator opcode` (bada70c) — `INTERP_TIME` opcode (#59): stereo-native per-channel ramps with linear/ease-in/ease-out/cosine curve modes via `inst.rate`. Handles NaN target (hold), time≤0 (passthrough), first-sample init. Verified by 11-sub-test Python experiment over 305 s / 14.6 M samples.
- `fix: SEQPAT_GATE retriggers per note` (e37336e) — Gate now drops to 0 for one sample at every event onset (explicit-overlap legato escape hatch via `dur(2)` preserved). New `test_seqpat_gate.cpp` covers contiguous retrigger, overlap-legato, gap, and cycle-wrap.
- `feat: .mid file playback` (f1dbb9f) — Runtime SMF parser (`smf_parser.cpp`) + file-playback in `MidiQueueState`; tempo-follow vs file, loop wrap, per-channel filter. Test fixtures (`twinkle.mid`, `tempo-map.mid`, `format2.mid`) generated by `generate.py`.
- `feat: poly() release: option` (b06465d) — `PolyAllocState` gains `release_window_samples`; `PolyVoice` gains `release_countdown`. VM mix loop holds `g=1.0` for the tail; resets on retrigger. 4 new `[poly-release]` cedar tests.
- `feat: midi() monophonic pipeline` (e275122) — `fill_mono_buffers` in `op_midi_query` fills per-block gate/freq/vel/trig buffers with last-note-wins + held-stack fallback. 6 new `[midi-mono]` cedar tests.
- `feat: soundfont() accepts midi() upstream` (7b71935) — `SoundFontVoiceState` gains `has_upstream_events`/`seq_state_id`/`preset_idx`; event-driven voice allocation via `trigger_sf_voices_for_note`. 4 new `[midi-soundfont]` cedar tests.
- `feat: ChannelCount::Match for control-rate utilities` (25e8bd8) — 11 control-rate utilities (slew, interp/interp_ease_in/ease_out/cos, env_follower, EDGE_OP family) switch from `Stereo` to `Match` so output width follows the primary signal input; fixes E186 mono-slot collision.

---

## akkado/

**Files touched:** ~39 | **Commits:** 10

- `feat: midi() builtin + codegen` (3bdfd44) — `midi(options?)` emits `MIDI_QUERY`, records `RequiredMidiSource`, returns `TypedValue::EventSource`. New `handle_poly_call` EventSource branch plumbs `state_id` into `PolyAllocState`. 9 source files; 46-assertion test suite.
- `feat: glide() + interp_* builtins` (da7a7b6) — Four `interp`/`interp_ease_in`/`interp_ease_out`/`interp_cos` builtins sharing `INTERP_TIME` via `inst_rate` dispatch; `glide(sig, time, curve, space)` stdlib fn with optional log-space pitch conversion.
- `feat: midi_cc routing` (f623949) — `midi_cc("name", {...})` builtin with `OptionSchema` (cc, channel, pb, at, min, max, slew); compile-time only, emits no bytecode. Errors E420/421/422/414/412/400.
- `feat: poly() release: option codegen` (b06465d) — Extracts `release` literal from poly call; plumbs through `StateInitData::poly_release_seconds`. 2 new `[poly-release]` akkado tests.
- `feat: soundfont() MIDI-upstream codegen` (7b71935) — `handle_soundfont_call` detects `is_runtime_event_source` or `ValueType::EventSource` upstream and emits a single `SOUNDFONT_VOICE` with all inputs `0xFFFF`. 2 new `[midi-soundfont]` codegen tests.
- `feat: midi() monophonic pipeline codegen` (e275122) — `PatternPayload` gains `is_runtime_event_source`; `handle_midi_call` returns `Pattern` so `pattern_field` dispatch resolves `e.freq`/`e.gate`/etc. without special-casing. 3 new `[midi-mono]` akkado tests.
- `feat: ChannelCount::Match` (25e8bd8) — `builtins.hpp` updated; mono-in assertions flipped in `test_types.cpp`; canonical glide example gets a positive test.
- `fix: canonicalize @ in error messages and autocomplete` (20c567d) — E003/E110/E400 now say `@` instead of `%`; `@.` triggers field completions in the editor.
- `fix: SEQPAT_GATE / drop .t alias` (e37336e) — `.t` alias removed (collided visually with `.time`/`.t0`); `.trig`/`.trigger` still resolve. New akkado test asserts `.t` produces E136.
- `feat: unison 1/sqrt(N) gain compensation` (85bcf2d) — `stdlib.hpp` scales voice sum by `1/sqrt(voices)` for RMS-constant output at any voice count (supersaw convention).

---

## web/

**Files touched:** ~130 | **Commits:** 16

- `feat: live Web MIDI input` (6e590c1) — `MidiInputController` in `midi-input.svelte.ts` lazily requests `MIDIAccess`, routes per-device events to the AudioWorklet via `cedar_push_midi_event` / `cedar_apply_midi_sources` WASM exports. `MidiInputPanel` in settings tab; device choice persists in `localStorage`.
- `feat: .mid file playback UI` (f1dbb9f) — `MidiInputPanel` gains drag-drop for `.mid` files; `midi-bank.ts` asset pipeline; `audio.svelte.ts` preload path; `cedar-processor.js` WASM bridge wiring.
- `feat: midi_cc routing WASM + Svelte` (f623949) — Seven `akkado_get_required_midi_cc_route_*` WASM exports; worklet `getRequiredMidiCcRoutes()`; main-thread dispatcher fans CC/PB/AT to `setParam` worklet message. Demo patch `midi-cc-filter.akk` (mod wheel → cutoff, CC2 → resonance, pitch-bend → ±2 semitones).
- `tweak: widen cutoff/res ranges in midi-cc-filter example` (fa7102a) — Cutoff 10–8000 Hz, resonance 0–12 for full musical range.
- `docs: glide & interp reference + concept page` (f5af8ab) — `utility.md` gains `glide`/`interp`/`interp_ease_in`/`interp_ease_out`/`interp_cos` sections with slew-vs-glide comparison table; new `concepts/glide-and-interpolation.md` concept page; `prd-glide-interp.md` status flipped to SHIPPED.
- `docs: use @ as canonical hole token` (e8421a9) — 59-file sweep of `web/static/docs/` and patches replacing `%` with `@` in prose, examples, and worklet comment.
- `docs: wrap long pipe chains, fix filter_lp → lp` (5b92302) — 33 doc/tutorial files reformatted so no `.akk` fence line exceeds ~70 chars; fixes five `filter_lp(...)` → `lp(...)` errors in `concepts/signals.md`.
- `docs: tighten tutorial and concept-doc prose` (02cc523) — Copyedits across 7 files; `manifest.ts` regenerated to pick up `runtime-controls` concept page.
- `docs: runtime-controls concept page` (c20488f) — New `concepts/runtime-controls.md` covering `param`, `button`, `toggle`, `dropdown` with parameter tables and worked `interactive-params.akk` example.
- `docs: fill v0.4.0 stereo-native gaps in signals page` (d1f483f) — Adds `pan(Stereo, Mono)`, `lfo_phase` extended param, spatializing FX on stereo, `sample()` always-stereo semantics.
- `docs: record-as-options cleanup` (dc4cd1e) — Drops stale "future spread" section and W160 hedge from `concepts/record-as-options.md`.
- `docs: unison reference + demo patches` (99fed60) — `reference/builtins/unison.md` with param table and four examples; `unison-lead.akk` and `unison-pad.akk` landing-page demos; `arrays.md` and `polyphony.md` cross-links.
- `fix: canonicalize @ in autocomplete` (20c567d) — `akkado-completions.ts` and `akkado-shape-index.ts` updated.
- `feat: ChannelCount::Match docs` (25e8bd8) — `glide-and-interpolation.md` updated to remove stereo-native workaround caveat.
- `feat: nkido-cli MIDI patches` (dd123da) — `web/static/patches/midi-keys.akk` example patch added.
- `fix: SEQPAT_GATE docs` (e37336e) — `reference/pattern/literals.md` and `tutorials/06-pattern-modulation.md` updated; `microtonal-raga.akk` switched from `.trig` to `.gate`.

---

## tools/

**Files touched:** ~22 | **Commits:** 5

- `feat: live MIDI input via RtMidi` (dd123da) — FetchContent `rtmidi 6.0.0` (gated `NOT EMSCRIPTEN`); `midi_input.{hpp,cpp}` with one `RtMidiIn` per physical port and mutex-guarded route table; CLI flags `--list-midi-devices` / `--midi-in <substring>`; reload integration at every `load_program` site.
- `feat: midi_cc dispatch` (f623949) — `dispatch_midi_cc` helper in `midi_input.hpp`; per-device CC route table; swap-on-bytecode-reload in `audio_engine`; new `test_midi_cc_dispatch.cpp` (5 cases, 14 assertions).
- `feat: .mid file playback` (f1dbb9f) — `audio_engine.cpp` asset pipeline for MIDI files; `program_loader.cpp` plumbing.
- `feat: soundfont MIDI-upstream plumbing` (7b71935) — `program_loader.cpp` wires `StateInitData::SoundfontEvents`.
- `feat: poly() release: option plumbing` (b06465d) — `program_loader.cpp` converts `poly_release_seconds` → samples.

---

## experiments/

**Files touched:** 3 | **Commits:** 3

- `test_op_interp_time.py` (bada70c) — 11 sub-tests for `INTERP_TIME`; 305 s long-stability run (0 NaN, 0 overshoot over 14.6 M samples). SLEW regression still green.
- `test_glide_pattern.py` (da7a7b6) — Pattern-driven glide test harness for the new `glide()` builtin.
- `test_op_unison.py` (85bcf2d) — Four sections: 5-voice spectrum, voices=1 balance, voices sweep {2,4,8}, 300 s poly+unison long-window (NaN/clipping/dropout guards). Peak dropped from 1.0 (clipping) to 0.5966 (= 1/√4).

---

## docs/ (root-level PRDs)

**Files touched:** ~58 | **Commits:** 13

- `PRD created: prd-midi-input.md` (45c10fb) — Full PRD for runtime MIDI input as poly event source.
- `PRD updated: prd-midi-input.md review fixes` (44732590) — Record-only call syntax, concrete `MidiRouteTable`, SPSC ordering, `tick_to_beat` walk, `init_midi_queue_state` signature.
- `PRD updated: prd-midi-input.md Phase 7` (7d59d37) — Inserts Phase 7 unification (Phases 7.1–7.6); pushes docs to Phase 8; spins runtime transforms off to sibling PRD.
- `PRD created: prd-glide-interp.md` (ab4103c) — PRD for time-based glide/interpolation system.
- `PRD updated: prd-glide-interp.md` (f5af8ab, 25e8bd8) — §3.4 stereo-native caveat documented; §3.4 removed after ChannelCount::Match fix; status → SHIPPED.
- `PRD created: prd-hole-field-shorthand.md` (411b45e) — Specifies `@field`/`%field` adjacency-gated shorthand; phased rollout and W201 warning.
- `PRD created: prd-distortion-roadmap.md` (c4c5342) — Absorbs squelch-engine DSP carryovers and adds hardclip/asym/foldback/quantize shapers, polymorphic `dist()`, multiband variant, Chebyshev polynomial shaper.
- `PRD updated: prd-unison.md marked DONE` (63bc059) — Phases 0a–0d and 2 checkboxes ticked; §11 acceptance criteria reconciled.
- `PRD updated: prd-stereo-native-opcodes.md` (25e8bd8) — §5.7 ChannelCount::Match added.
- `docs: mini-notation-reference SEQPAT_GATE` (e37336e) — `.trig` (pulse) and `.gate` (held + retrigger) documented as distinct fields; legato-via-`dur()` example added.
- `docs: adopt @ canonical in CLAUDE.md and 42 active PRDs` (f82e591) — Sweeps `%` → `@` across CLAUDE.md and all in-progress PRDs; dual-token references preserved in design sections of the hole-field and closure-pipe PRDs.
