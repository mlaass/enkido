# Daily Summary — 2026-07-05

**Commits:** 8 (excluding 1 merge commit)  
**Contributors:** Moritz Laass  
**Window:** 2026-07-04 14:06 → 2026-07-04 23:21 CEST

---

## akkado/

**Files touched: 15**

- **feat(akkado): scale-quantize audit + Phase 4 user-defined scales** (`9f39460`)  
  Added `scale(pat, root, ivals)` / `key(pat, root, ivals)` arity-3 stdlib overloads and the compile-time builtin `key_deltas(root, ivals)`. Fixed `key()` to accept and ignore octave digits (`"d2:minor"` now equals `"d:minor"`). Refactored generated dispatchers to delegate to shared `key_q`/`scale_q` functions with literal tables — stdlib shrinks 94 KB → 75 KB and `[drift_fuzz]` time drops 7.5 s → 4.2 s. Added transitive param-literal propagation in codegen. Full test suite: 1211 cases / 321 k assertions green.

- **fix(cedar): clock-derived alternation — hot-swapped patterns join in phase** (`9d705a3`)  
  `codegen_patterns.cpp` and `state_init_buffer.hpp` updated: each `Sequence` now carries a compile-time `steps_per_cycle` so `query_pattern()` re-derives the current step as a pure function of the clock, eliminating the monotonic-counter bug where hot-swapped patterns started at element 0 out of phase. Wire format bumped WIRE_VERSION 1 → 2; TS codec regenerated.

- **docs: add EBNF grammar reference; sweep stale chord/pattern syntax** (`ae5b398`)  
  Removed stale `C4'` apostrophe chord literals from parser comments (`music_theory.hpp`, `parser.hpp`, `parser.cpp`). CLAUDE.md chord section rewritten to reflect reality.

---

## cedar/

**Files touched: 2**

- **fix(cedar): clock-derived alternation — hot-swapped patterns join in phase** (`9d705a3`)  
  `cedar/include/cedar/opcodes/sequence.hpp` extended with `steps_per_cycle` field. `cedar/include/cedar/vm/state_pool.hpp` updated accordingly. Regression tests added in `akkado/tests/test_pattern_playback_e2e.cpp` covering fresh-state phase join, same-cycle idempotency, `*N` sub-cycle derivation, and nested alternation order.

---

## web/

**Files touched: 7**

- **test(e2e): fix hot-swap spec races; re-enable unison-pad continuity test** (`9e5095c`)  
  `web/e2e/hot-swap-audio.spec.ts` reworked: `bootAudioAndCompile` now retries `setCode+evaluate` until the target source verifiably compiles (fixing two race conditions where the baseline captured the welcome patch). Waits for audible output instead of a fixed 500 ms delay. Unison-pad continuity test re-enabled after 20-run verification; full e2e suite 15/15 green.

- **fix(cedar): clock-derived alternation** (`9d705a3`)  
  `web/scripts/build-state-init-codec.ts` and `web/src/lib/audio/state-init-codec.ts` updated for WIRE_VERSION 2, adding `steps_per_cycle` to sequence records.

- **feat(akkado): scale-quantize audit + Phase 4** (`9f39460`)  
  `web/scripts/generate-scale-quantize.ts` and `web/src/lib/docs/manifest.ts` updated; new reference page `web/static/docs/reference/pattern/scale-key.md` added with F1 keywords.

---

## docs/

**Files touched: 18+**

- **docs: engine-side PRDs for nkido studio + juce-plugin repo amendment** (`e75639f`)  
  Three new engine-side PRDs added: `prd-multi-program-host.md` (N VMs on a shared clock, v1 single-program with N-ready interfaces), `prd-capture-recording.md` (per-bus host tap surface + future `record()` opcode), `prd-plugin-node-latency.md` (per-node latency declaration + report-only PDC, also flags `DYNAMICS_LIMITER`'s unreported lookahead delay). `prd-juce-plugin.md` §3/§11.7 amended: standalone juce-plugin repo superseded by closed nkido-studio repo.

- **docs: audit + retire stale reference docs** (`cf3a1da`)  
  Pre-C++ Rust/petgraph design documents (`Akkado A Rhythmic & DSP Language Specification.md`, `synth-overview.md`) retired to untracked `docs/archive/` with SUPERSEDED banners. `mini-notation-implementation.md` corrected (timing section, SEQ_STEP → SEQPAT opcode family, UI step highlighting marked shipped). `mini-notation-reference.md` modernised (top-level = per-cycle alternation, apostrophe chord literals removed, `%` holes updated to `@`, x/micro accidentals and two-digit octaves documented).

- **docs: add EBNF grammar reference; sweep stale chord/pattern syntax** (`ae5b398`)  
  New `docs/grammar/` directory with three EBNF files: `akkado-grammar.md` (core language with precedence table and parser desugarings), `mini-notation-grammar.md`, `chord-grammar.md`. Stale `C4'` syntax swept from `docs/prd-closure-pipe-operator.md`, `docs/prd-compile-time-functions.md`, `docs/prd-mini-notation-micro-timing.md`, `docs/prd-soundfont-poly-unification.md`, and `docs/agent-guide-userspace-functions.md`.

- **docs: mark prd-scale-quantize COMPLETE** (`a7965a6`)  
  Status line updated to `DONE`.

- **feat(akkado): scale-quantize audit** (`9f39460`)  
  New `docs/audits/prd-scale-quantize_audit_2026-07-04.md` (179 lines). `docs/prd-scale-quantize.md` synced to shipped reality (catalog descope, identity fallback, underscore names, stale §7 codes removed).

---

## other (root)

**Files touched: 2**

- **Release v0.4.4: add top-level MIT LICENSE** (`cdc968641`)  
  Added `LICENSE` (MIT, 21 lines). `VERSION` bumped to `0.4.4`. Named as a prerequisite for the nkido-studio `prd-studio-foundation` §4.1 critical-path item — the external/nkido submodule pin requires a real license grant in the tree.
