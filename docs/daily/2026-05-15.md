# Daily Summary — 2026-05-15

**Commits**: 6  
**Contributors**: Moritz Laass  
**Window**: 2026-05-14 13:59 → 16:20 CEST

---

## cedar/

**Files touched**: 9

- **feat(stereo): Phase 5 stereo-native opcodes — retire auto-lift** (`6a1d3fa`)  
  Converted the last `auto_lift=true` holdouts (`slew`, `sah`, `gateup`, `gatedown`, `counter`) to stereo-native, then deleted the auto-lift dispatch branch entirely. `SlewState`/`EdgeState` gain `[2]` per-channel arrays; `STEREO_STATE_XOR_R` removed from `instruction.hpp`; the `'11'` STEREO_INPUT flag now exclusively marks stereo-native opcodes. New cedar tests cover stereo-native dispatch for EDGE_OP and SLEW families.  
  _Files_: `cedar/include/cedar/opcodes/dsp_state.hpp`, `edge_op.hpp`, `utility.hpp`, `cedar/include/cedar/vm/instruction.hpp`, `state_pool.hpp`, `cedar/src/vm/vm.cpp`, `cedar/tests/test_edge_op.cpp`

- **fix(stereo): preserve stereo through poly voice mixing** (`6b44b4b`)  
  `poly` was collapsing stereo voices to mono — panned voices played back centred. Fixed by making `poly` stereo-native: `voice_out` and `mix` are now adjacent L/R pairs, `POLY_BEGIN` carries `STEREO_OUTPUT`, and `execute_poly_block` accumulates both channels. Mono bodies broadcast dual-mono; stereo bodies keep both channels.  
  _Files_: `cedar/src/vm/vm.cpp`, `cedar/tests/test_poly.cpp`

---

## akkado/

**Files touched**: 5

- **feat(stereo): Phase 5 stereo-native opcodes — retire auto-lift** (`6a1d3fa`)  
  Removed the `is_stereo_expansion` codegen path and `/L` state-id suffix logic from `codegen.cpp`; dropped the `auto_lift` field from `BuiltinInfo` and all initializers in `builtins.hpp`; added akkado tests for slew/edge-family stereo codegen. E186 channel-mismatch check now keys only on `stereo_native`.  
  _Files_: `akkado/src/codegen.cpp`, `akkado/tests/test_codegen.cpp`, `akkado/tests/test_types.cpp`

- **fix(stereo): preserve stereo through poly voice mixing** (`6b44b4b`)  
  Updated `builtins.hpp` for poly's new stereo-native signature; extended `codegen_functions.cpp` to emit `STEREO_OUTPUT` on `POLY_BEGIN` and handle stereo voice-out COPYs; new akkado codegen test for stereo propagation to `out()`.  
  _Files_: `akkado/include/akkado/builtins.hpp`, `akkado/src/codegen_functions.cpp`, `akkado/tests/test_codegen.cpp`

---

## web/

**Files touched**: ~56

- **docs(web): use out(%) idiom instead of out(%, %)** (`11be6c5`)  
  Swept ~50 web documentation files to replace the outdated `out(%, %)` / `out(@, @)` double-hole forms with the correct `out(%)` single-stereo-arg idiom. Genuine two-distinct-argument examples (`out(left, right)`) left untouched. Covers tutorials, language reference, mini-notation, pattern, and URI-schemes docs.  
  _Files_: 50 files under `web/static/docs/`

- **feat(stereo): Phase 5 stereo-native opcodes** (`6a1d3fa`)  
  Updated `web/src/lib/docs/manifest.ts` and stereo/signals/reverbs/audio-input docs to reflect the completed auto-lift removal and Phase 4/5 stereo PRD status.  
  _Files_: `web/src/lib/docs/manifest.ts`, `web/static/docs/concepts/signals.md`, `web/static/docs/reference/builtins/audio-input.md`, `web/static/docs/reference/builtins/reverbs.md`, `web/static/docs/reference/builtins/stereo.md`

- **Release v0.4.0** (`936fb81`)  
  Version bump via `bump-version.sh`.  
  _Files_: `web/package.json`

---

## docs/

**Files touched**: 4

- **docs: 0.4.0 changelog entry** (`b97c75b`)  
  Promoted the `[Unreleased]` section in `CHANGELOG.md` to `[0.4.0] - 2026-05-14`, covering the stereo-native opcode migration, ExtendedParams, record destructuring, microtonal tuning, the unified URI resolver, nkido-cli serve mode, and the poly stereo voice-mixing fix.  
  _Files_: `CHANGELOG.md`

- **feat(stereo): Phase 5 stereo-native opcodes** (`6a1d3fa`)  
  Updated design docs to mark both stereo PRDs as COMPLETE; refreshed cedar-architecture.md to reflect the retired auto-lift mechanism.  
  _Files_: `docs/cedar-architecture.md`, `docs/prd-stereo-native-opcodes.md`, `docs/prd-stereo-support.md`

---

## experiments/

**Files touched**: 1

- **feat(stereo): Phase 5 stereo-native opcodes** (`6a1d3fa`)  
  Updated `test_op_stereo_input_flag.py` to drop stale auto-lift wording and align with the new stereo-native flag semantics.  
  _Files_: `experiments/test_op_stereo_input_flag.py`

---

## other/

**Files touched**: 2

- **docs: document bump-version.sh release workflow in CLAUDE.md** (`abecc9a`)  
  Added the canonical release workflow to `CLAUDE.md`: write changelog, run `bump-version.sh`, push with `--tags`. Documents the guard conditions (clean tree, no pre-existing tag, matching CHANGELOG entry) and the correct ordering.  
  _Files_: `CLAUDE.md`

- **Release v0.4.0** (`936fb81`)  
  `VERSION` updated to `0.4.0`.  
  _Files_: `VERSION`

---

## Highlights

The session's dominant theme was completing the **stereo-native opcode migration** (Phases 4–5): the auto-lift dispatch mechanism is now fully retired, `poly` correctly preserves stereo through voice mixing, and all documentation uses the `out(%)` idiom. This work culminated in the **v0.4.0 release**, with a full changelog entry and release workflow documentation added to CLAUDE.md.
