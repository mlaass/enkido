# Daily Summary 2026-05-17

**22 commits** · 1 contributor (Moritz Laass)

---

## cedar/

**11 files touched**

- **feat(cedar, web): prd-midi-input Phase 7.3 / 7.4 / 7.6 + mono retrigger fix** (56c0ae0e) — `MidiQueueState` gains held-note migration support and `last_file_uri` for hot-swap continuity; `smf_parser` collects CC/PB/channel-AT events; `advance_file_cc_into_output` drives file CC playback through existing route tables; mono retrigger hang fixed by replacing float-precision timing comparisons with an authoritative `MonoTransitionBuf`. 8 files: `cedar/include/cedar/{io/midi_sequence.hpp,opcodes/midi.hpp,vm/{state_pool,vm}.hpp}`, `cedar/src/{io/smf_parser,opcodes/midi,vm/vm}.cpp`, `cedar/tests/test_midi.cpp`.

- **fix(soundfont): CLI auto-load, release envelope, patch gain staging** (a27270e8) — Release-stage compounding bug fixed by capturing `env_release_start_level` at gate-off and using `start * exp(-5p)` instead of the iterative `env_level` self-multiply that collapsed 100ms releases to ~10ms. 2 files: `cedar/include/cedar/opcodes/{soundfont,dsp_state}.hpp`.

- **docs(midi): prd-midi-input Phase 8** (2c3a531b) — Regenerated `cedar/include/cedar/generated/opcode_metadata.hpp` via `build:opcodes`.

---

## akkado/

**3 files touched**

- **fix(akkado): modifiers (`*` `/` `@` `!` `?`) inside `n"…"` / `v"…"` patterns** (96ebd8bc) — Mini-notation lexer's `value_mode_` / `note_mode_` paths routed digit-leading modifier arguments through `lex_value_atom` / `lex_note_atom`, causing `g4@1.25` to tokenise as two pitch tokens rather than a pitch + modifier argument. Fixed by tracking whether the previously-emitted token was a value-taking modifier and skipping typed-prefix atom paths when set. Regression test added for all five modifiers in both modes. Files: `akkado/include/akkado/mini_lexer.hpp`, `akkado/src/mini_lexer.cpp`, `akkado/tests/test_mini_notation.cpp`.

---

## web/

**~80 files touched** (across 16 commits)

### Shareable Patches — full feature shipped (Phases 0–4)

- **Phase 0 + 1: inline URL sharing + multi-draft persistence** (e63a1aa0) — `encodeInlineUrl` / `decodeInlineHash` via lz-string; `/p/[[slug]]` viewer route; `StorageProvider` interface + `LocalDraftProvider` (localStorage drafts + recently-visited); `drafts.svelte.ts` Svelte 5 rune store with phantom-tab lifecycle; `EditorTabs.svelte` tab bar; `PatchesPanel.svelte`; `Editor.svelte` Ctrl+T / Ctrl+Shift+W bindings. 20 files, 48 new tests.

- **Phase 2 chunk 1: Cloudflare Worker scaffold** (3cfa57a4) — Hand-rolled ~150 LOC router over D1 SQLite: `POST /share` (8-char base62 slug), `GET /api/p/:slug`, `GET /p/:slug` (OG-tagged HTML + meta-refresh), `POST /report` stub. 27/27 Miniflare tests pass. 15 files under `web/share-api/`.

- **Phase 2 chunk 2: WorkerShareProvider + ShareDialog + Share/Fork buttons** (6ec0f41f) — `WorkerShareProvider` composes `LocalDraftProvider`, proxies share/fetch/report to `PUBLIC_SHARE_API_BASE`; `getProvider()` factory memoized per env-base; `ShareDialog.svelte` with Permalink/Inline-link tabs; header Share/Fork buttons. 6 files.

- **Phase 2 chunk 3: slug viewer + slug-phantom drafts helpers** (915b0916) — Real fetch-and-open flow at `/p/<slug>`; `openSlugPhantomTab`, `findSlugPhantom`, `deleteSlugPhantom` on the drafts store; reuses existing phantom on revisit per PRD §10.3. 12 new tests. 3 files.

- **Phase 2 chunk 4: Recently visited + forgetVisited** (b66b4ad9) — `StorageProvider` gains `forgetVisited(slug)`; `PatchesPanel` "Recently visited" section with MRU order, local-phantom indicator, Forget (confirm). 8 files.

- **Phase 3: Report endpoint + UI + runbook** (76342ee9) — `handleReport` parses/validates `{slug, reason?}`, COALESCE preserves first-report timestamp, filters soft-deleted slugs; `ReportDialog.svelte` with `reported-slugs.ts` localStorage dedup; Flag button in header for phantom slugs only. 12 new Worker tests. 8 files.

- **Phase 4: self-hosting README polish** (7e07192d) — Full self-hosting walkthrough in `web/share-api/README.md`; "Hosting your own share endpoint" section in `web/README.md`. 2 files.

### Unified Files Panel (prd-unified-files-panel)

- **Phase A: unified Files panel** (d61cbd93) — Collapses Files + Samples tabs into one; drop zone + URL loader; three collapsible sections (Samples / SoundFonts / MIDI); audio store registries now reactive arrays tagged with `origin: 'builtin' | 'user'`. 8 files.

- **Phase B: persist user file uploads across reloads** (ce9e7fdf) — `file-cache` v2 adds `pinned` flag; `upload-manifest` wrapper; `file-router` persists bytes on drop; `audio.svelte.ts` serial restore loop in `initialize()` so first compile sees hydrated assets; `[×]` remove button on user-origin rows. 5 files.

- **Files panel polish** (f6ff1945) — Lucide `ChevronRight/Down` icons replace Unicode chevrons; copy-filename button (Copy → Check flash) per row; in-browser MIDI scanner (`midi-meta.ts`) surfaces per-channel info (GM program name, note count, Ch 10 = Drum Kit). 6 files.

### Web audio fixes

- **fix: eager audio init** (a69ac454) — `initPromise` memoization so concurrent `onMount` + first file drop await the same bootstrap; `loadSample/Font/Midi/Wavetable` each `await initialize()` first; `+layout.svelte` fires `initialize()` on mount so WASM + worklet load during page-load window. 2 files.

- **fix: re-upload MIDI files after Stop** (729cf77a) — `stop()` now refetches MIDI bytes from `midiBank` blob URLs and re-pushes to worklet after `VM::reset()` wipes `midi_sequences_`; without this, the compile-time `loadedMidiFilesIndex` short-circuit silently left the worklet with no sequences. 1 file.

### Patches / examples

- **feat: expand midi-cc-filtermono example** (2c4f1150) — Adds CC 3 (PWM) + CC 4 (glide time); switches to `saw_pwm` + `sqr_pwm` with phase-sync via `@.trig`; velocity smoothed over 2ms; `saturate(2)` for soft clip. 1 file.

---

## tools/

**11 files touched**

- **fix(soundfont): CLI auto-load, release envelope, patch gain staging** (a27270e8) — `prepare_program_assets()` now iterates `cr.required_soundfonts` before `--soundfont` URIs; `resolve_soundfont_alias()` maps `gm` / `gm_medium` / `gm_large` to bundled SF3s; `--no-default-soundfont` opt-out; install rule for bundled SF3s. `nkido-cli play soundfont-play.akk` (no `--soundfont`) was previously silent; now produces clean audio (peak 0.378, zero clipping). Files: `tools/nkido-cli/{asset_loader.{hpp,cpp},bytecode_loader.hpp,main.cpp,program_loader.cpp,CMakeLists.txt,tests/{fixtures/soundfont_gm.akk,test_render.cpp}}`.

- **feat(cedar, web): prd-midi-input Phase 7.4** (56c0ae0e) — CLI publishes `FileCcDispatchPlan` and drains via existing CC route table after `process_block`. Files: `tools/nkido-cli/{audio_engine.{hpp,cpp},main.cpp}`.

---

## experiments/

**1 file touched**

- **fix(soundfont): test_op_soundfont_release.py** (a27270e8) — New experiment validates the release-shape fix and runs 599 s of stress coverage (92.5% envelope level at +10 ms post-release; was ~0% pre-fix).

---

## docs/

**6 files touched**

- **docs(prd-soundfont-poly-unification): new execution PRD** (c2a2eb7a) — Consolidates SF_VOICE single-voice opcode, envelope-done stealing, poly callback event records, and per-event MIDI channel/program into one unified future PRD. 602 insertions.

- **docs(prd-glide-interp): reflect shipped state** (878f8114) — All four phases marked DONE with commit hashes; `ChannelCount::Match` noted as shipped form; stdlib sketch updated to inlined nested-match; risk §10.1 resolution documented.

- **docs(prd-shareable-patches): review fixes + Phase 0** (13078660) — Corrected localStorage key, netlify.toml path, route references; added Phase 0 inline URL sharing section, migration tiebreaker, embed-mode bypass edge case.

- **docs(prd-unified-files-panel): Stop/reset MIDI asymmetry** (3a654a0c) — Documents that `VM::reset()` wipes MIDI sequences but not sample/soundfont/wavetable registries; lists three acceptable Phase B restoration paths.

- **docs(dsp-issues): mono MIDI pop on rapid retriggers** (378d88d2) — Captures diagnostic path and userspace workaround (`glide(@.vel, 0.002)`) for pops that remained after a failed gate-drop attempt.

- **docs(midi): prd-midi-input Phase 8** (2c3a531b) — New reference pages `midi.md`, `midi-cc.md`; new tutorial `tutorials/07-midi.md`; cross-links added to `polyphony.md`, `soundfonts.md`, `uri-schemes.md`, `audio-input.md`; `manifest.ts` regenerated.
