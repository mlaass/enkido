# Daily Summary — 2026-06-08

**Commits:** 5 | **Contributors:** Moritz Laass

## akkado/ — 3 files touched

- **test: fix pre-existing red tests** (`bdc7914`) — Repaired three test sources corrupted by the earlier `osc()`→dedicated-builtins migration: restored correct function names (`my_osc`, `sine`→`osc`) so tests exercise what they claim. Full akkado C++ suite green (1135 cases).
- **feat(web): state-init codec validator** (`cc1d09f`) — Added `static_assert` size pins for `cedar::Event` (240 bytes) and `TimelineState::Breakpoint` (12 bytes) in `state_init_buffer.hpp`; restored `test_state_init_buffer_codec.cpp` to the CMake build (7 cases, including bit-perfect pad hot-swap).

## web/ — 8 files touched

- **test: fix pre-existing red tests** (`bdc7914`) — Fixed `arrays.test.ts` (`arr` now a reserved type-annotation keyword; binding renamed to `xs`). Migrated `sample-velocity.test.ts` from removed exports to the `*_from_buffer` API (matching the worklet's `runProgramLoad`). Added `akkado_compile_bypass_master` WASM export to suppress the master-bus `DISTORT_SOFT` soft-clip during tests, enabling exact linear velocity assertions.
- **feat(web): state-init codec validator + round-trip test** (`cc1d09f`) — New `web/scripts/build-state-init-codec.ts` generator parses C++ headers and emits `web/src/lib/audio/state-init-codec.ts` (decoder + WIRE_VERSION + size constants). New `web/tests/state-init-codec.test.ts` loads real `nkido.wasm`, packs via C++, walks bytes with the TS decoder, and asserts exact buffer consumption — fails on any wire-format drift.

## docs/ — 3 files touched

- **docs: add JUCE plugin architecture PRD** (`e785a7a`) — New 734-line `docs/prd-juce-plugin.md` outlining a closed-source JUCE 8 plugin (VST3/CLAP/Standalone/AU) that runs Cedar/Akkado natively and embeds the existing web UI via JUCE WebView. Covers shared-UI core extraction, host transport sync, 128-block accumulator, 64-slot automation pool, presets, and a phased Linux-first rollout.
- **docs(prd-juce-plugin): review fixes** (`bbc1ce3`) — Corrected `in_l` → `in()` builtin reference, fixed `midi-input.svelte.ts` path, settled engine license as MIT, unified parameter terminology to "macro slots", added §5.6 for plugin-only UI panels under `web/src/lib/plugin/` with `IS_PLUGIN` build flag, renumbered open questions.
- **feat(web): mark compile-off-audio-thread PRD shipped** (`cc1d09f`) — Updated `prd-compile-off-audio-thread.md` status to `IMPLEMENTED 2026-05-29 (v0.4.3)`; rewrote §5.4 to reflect the as-built C++-codec + TS-validator design.
- **docs: fix PRD status + document conventions** (`9aa027c`) — Fixed `prd-parser-codegen-correctness.md` status headline (singular "Phase 0" was tripping the `in-progress` heuristic). Added a PRD Status Conventions section to `CLAUDE.md` documenting recognized verdict keywords and the singular-Phase pitfall.

## other/ — 1 file touched

- **docs: fix prd-parser-codegen-correctness status, document PRD conventions** (`9aa027c`) — `CLAUDE.md` gains a new section on PRD status conventions and the `list-prds.py` bucketing rules.
