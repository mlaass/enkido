# Daily Summary — 2026-05-09

**Commits**: 9 (excluding bot meta-commits)
**Contributors**: Moritz Laass

---

## akkado/

**fix(chord) — unify quality table, expand voicings, decouple close/open** (`e3d8163`)
Merged the duplicate `CHORD_QUALITIES` (chord_parser.cpp) and `CHORD_INTERVALS` (music_theory.hpp) tables into a single canonical source, eliminating silent truncation of chords whose quality was absent from the partial table (e.g. `CM7` → 3-note triad). Bumped `MAX_VALUES_PER_EVENT` 4 → 16 so extended voicings with 5+ notes (M9, C13, …) no longer truncate. Fixed close/open/drop2/drop3 transforms to apply *after* inversion rather than before, giving open voicings ~9× more sub-bass separation. `max_voices` in `emit_pattern_with_state` now derives from post-voicing sequence events instead of the stale parser-time count, fixing soundfont path voice truncation. New tests cover extended/alt qualities at parser and codegen+soundfont levels.
*Files touched*: 8 akkado + 1 cedar + 1 experiments (10 total, +574/−146)

**fix(chord-soundfont) — pipe chord patterns natively into soundfont** (`77e7735`)
`PatternPayload` now carries `voice_freqs[]` for all chord voices; `emit_per_voice_seqpat` actually emits one `SEQPAT_STEP` per voice (previously only voice 0 was published). `soundfont` opts in via a new `consumes_polyphonic_pattern` builtin flag — it erases the pattern from polyphonic_pattern_nodes_ to suppress E410, then emits one `SOUNDFONT_VOICE` per chord voice (each with its own `state_id`) summed with ADD opcodes. Mono-synth chains still produce E410. 4 new Catch2 cases / 43 assertions added; Python experiment verifies each chord-voice fundamental is energetically present (≥25% as loud as the loudest voice).
*Files touched*: 4 akkado + 1 experiments + 2 web (7 total, +647/−93)

**feat(records) Phase 5 — options helper migration + records reference docs** (`52ce223`)
New `akkado/include/akkado/codegen/options.hpp` introduces `OptionValue`, `OptionsPayload`, and `extract_options()` to centralise record-as-options parsing. All 5 visualizer codegen handlers migrated to `extract_viz_options()`. 6 new test cases under `[codegen][viz][options-helper]`. New web docs: `reference/language/records.md` and `concepts/record-as-options.md`; cross-links added to `state.md`, `variables.md`, `arrays.md`. Docs lookup index rebuilt (50 docs, 515 entries).
*Files touched*: 7 akkado + 6 web + CLAUDE.md (14 total, +878/−136)

**feat(records) Phase 4b — `cell.field` read/write sugar over record cells** (`5b9358c`)
Bidirectional desugaring of `cell.field` (→ `STATE_OP rate=1`) and `cell.field = expr` (→ `STATE_OP rate=2`) against per-field sub-cells. New `FieldAssignment` AST node; parser transforms a `FieldAccess + =` into a `FieldAssignment` and rejects pipe-RHS writes at parse time. New `is_state_cell` flag on `Symbol` bypasses the analyzer's E061 gate for state-cell variables. New diagnostics: E204 (nested-field write rejected), E205 (pipe-position write rejected); E150 extended with `state({...})` hint. 12 new tests. Result: 659 akkado test cases all green; cedar unchanged.
*Files touched*: 8 akkado + 1 web (9 total, +615/−11)

---

## cedar/

**fix(chord) — raise `MAX_VALUES_PER_EVENT`** (`e3d8163`, shared with akkado above)
`cedar/include/cedar/opcodes/sequence.hpp`: `MAX_VALUES_PER_EVENT` increased from 4 to 16, unblocking extended chord voicings.
*Files touched*: 1 (+7/−1)

---

## experiments/

**Chord pipeline audio tests extended** (`e3d8163`, `77e7735`)
`test_chord_pipeline.py` substantially expanded across two commits: tests now cover all four user chord pipelines (`chord()`, `c"…"`, apostrophe literals, `n"…"` baseline), render WAV files, and assert each chord-voice fundamental is energetically present (≥25% of the loudest voice). Baseline `n"c4 a4 d4 g4"` confirms detector tightness (e4/g4 ratios 0.4% / 0.0%).
*Files touched*: 1 (+416 net across both commits)

---

## docs/

**docs(chord) — sync chord-quality docs with unified `CHORD_INTERVALS`** (`7e39e9f`)
Chord reference expanded to cover every alias in the canonical table: triads (`M`, `-`, `+`, `o`), sevenths (`^`, `^7`, `m7b5`, `0`, `mM7`), 6ths, 9/11/13 extended. Stale 4-voice limit note in `chords.md` fixed (`MAX_VALUES_PER_EVENT` is now 16). Language spec EBNF updated: `quality|extension` split replaced with a single non-terminal enumerating all valid keys, with a note that apostrophe-literal, `chord()`, and `c"..."` share one table. F1 lookup index regenerated.
*Files touched*: 4 (+339/−56)

**docs — PRD for `->>` closure-pipe operator** (`6848a44`, `5c22521`)
New 708-line PRD for the `->>` (closure-pipe) operator created, then revised to resolve factual errors and internal contradictions surfaced in review: closure always occupies the last positional slot; desugar to a named argument keyed on the closure-typed slot; bare-% multi-param contradiction resolved in favor of W150 warning; field-access restricted to `%.field`; RHS parses at `Precedence::Or`; impact assessment updated to mark codegen, builtins, tests, web docs, and experiments as Modified; fixed typos and a braces example.
*Files touched*: 1 (net +333/−109 after revision)

**docs(records) Phase 6 — PRD audit, status flipped to DONE** (`2109713`)
Audit document confirms all 10 goals met, all 6 phase gates and 7 whole-PRD gates pass, all 5 open questions resolved or deferred; coverage meets PRD §11.1 targets across every tag. `prd-records-system-unification.md` status field updated to DONE.
*Files touched*: 2 (+169/−2)

---

## web/

**Docs and index updates** (across `7e39e9f`, `77e7735`, `52ce223`, `c42391a`, `5b9358c`)
- `reference/mini-notation/chords.md`: chord quality table expanded, soundfont piping documented, code snippets cleaned up.
- `reference/builtins/filters.md`: code snippets cleaned up.
- `reference/language/records.md`: new comprehensive records reference.
- `concepts/record-as-options.md`: new concept doc for the record-as-options convention.
- `reference/builtins/state.md`: Phase 4b `cell.field` examples added.
- `src/lib/docs/manifest.ts`: lookup index rebuilt multiple times to incorporate new and updated docs (50 docs, 515 entries at last rebuild).

*Files touched*: 8 net unique web files
