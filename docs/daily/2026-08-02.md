# Daily Summary — 2026-08-02

**Commits:** 14 &nbsp;|&nbsp; **Contributors:** Moritz Laass &nbsp;|&nbsp; **Period:** 2026-08-01 15:00–19:03 UTC+2

---

## akkado/

**14 commits — ~120 file-changes**

Two large refactor PRDs landed and were closed out today: **prd-parser-codegen-hardening** (Phases 6–9, now DONE) and **prd-codegen-sprawl-cleanup** (Phases 1a–7, now DONE). All commits are verified byte-identical across the 36-file corpus; full akkado + cedar suites remain green throughout.

### prd-parser-codegen-hardening — Phases 6–9 (DONE)

- **Phase 6** (`bec56967`) — *Ghost-field migration*: migrated 4 node-ref ghost fields out of AST `Data` variants into a uniform `Node::extra_children[]` slot (PRD-14). `MatchArmData::guard_node`, `ArgumentData::spread_source`, `RecordLit` spread source, and `DestructureField::default_node` all moved to indexed extra-children. New `destructure_field.hpp` (`DestructureBinding` + `destructure_bindings()` zip helper). `clone_subtree` and `substitute_nodes` drop ~120 LOC of per-Data special-casing for one generic loop each. New `test_ast.cpp` [P6]; 1247 cases green.

- **Phase 7** (`bb9a512d`) — *expr_kinds + named_args consolidation* (PRD-13): new `expr_kinds.hpp/.cpp` centralises `midi_to_hz`, `is_pattern_producing_call_name`, `is_pattern_producer`, `is_literal_value`, and `try_const_value`. New `named_args.hpp/.cpp` implements `assign_named_arg_slots()` — the analyzer's two 142-line reorder overloads collapse to ~150 LOC caller-side. New `test_expr_kinds.cpp` + `test_named_args.cpp`; 1264 cases green.

- **Phase 8** (`26f92c9f`) — *shape_index over CompileArtifacts* (PRD-2): deleted the re-lex+re-parse pipeline from `shape_index.cpp`; `serialize_shape_index(artifacts, cursor)` replaces `shape_index_json(source, cursor)` — zero lexer/parser calls remain in `shape_index.cpp`. `CompileArtifacts` grows `parsed_ast`, `user_source_offset`, `user_source_hash`, and `interner`. Web: `akkado_get_shape_index()` moved to the compile worker; the AudioWorklet's `getShapeIndex` handler deleted (worklet-contract win). Full suite + 164 web unit + 15 Playwright e2e green.

- **Phase 9** (`858c41c6`) — *Close-out*: parallelism-blocker-catalogue reduced to one open entry (PB-F5, out of scope — PRD-3). PRD status flipped to DONE; commit hashes backfilled for all 9 phases. Audit doc executive summary updated with full hardening resolution tally.

### prd-codegen-sprawl-cleanup — Phases 1a–7 (DONE)

- **Phase 1a** (`fd76fd16`) — *InstructionBuilder + single E101 path*: new `codegen::InstructionBuilder` (fluent `cedar::Instruction` construction, inputs default to `0xFFFF`) and `CodeGenerator::alloc_buffer(loc)` as the single "Buffer pool exhausted" E101 diagnostic. New unit tests for builder field defaults, slot setters, `BufferAllocator` exhaustion/release, and end-to-end E101 compile.

- **Phase 1b** (`27fb66e7`) — *Emission site migration*: ~120 instruction-emission sites and ~60 bare `allocate + E101` blocks across all ten `codegen*.cpp` files migrated to `InstructionBuilder` / `alloc_buffer`. Bare `0xFFFF` sentinels reduced 496 → 108; `'Buffer pool exhausted'` literals 166 → 76. Dead `set_unused_inputs` helper deleted.

- **Phase 2** (`ff8fecb1`) — *StateInitBuilder*: new `codegen::StateInitBuilder` unifies all 18 field-by-field `StateInitData` construction sites. `state_inits_.push_back` now has exactly one call site (`push_state_init`). `extended_params` factory pre-fills `ext_buffer_indices` with `0xFFFF`. Unit tests for every factory.

- **Phase 3a** (`30ca0e3b`) — *Source layout*: all `codegen_*.cpp` files moved to `akkado/src/codegen/<concern>.cpp` with the `codegen_` prefix dropped. Placeholder TUs registered in CMake. Pure move, no code change.

- **Phase 3b** (`0c6eaf15`) — *File splits*: `codegen.cpp` (4184 → 606 LOC) splits into `visit_dispatch.cpp`, `bus.cpp`, `records.cpp`, and core plumbing. `patterns.cpp` (5908 → 960 LOC) splits into `pattern_compiler.hpp` (header-only `SequenceCompiler`), `pattern_transforms.cpp`, `pattern_io.cpp`, and the mini-literal/SEQPAT core. Seven shared statics promoted to inline functions in `codegen/helpers.hpp`.

- **Phase 4a** (`388245ce`) — *special_handlers map deleted*: 84-entry name-keyed `special_handlers` map removed from `visit()`'s `Call` branch; each `BUILTIN_FUNCTIONS` entry now carries `.codegen_handler = BuiltinHandlers::<handler>`. New `builtin_info.hpp` splits `BuiltinInfo` + enums out of `builtins.hpp` to enable member function pointers. `BuiltinKind` enum added for Phases 5/6. Zero `grep special_handlers` hits repo-wide.

- **Phase 4b** (`4ed80b63`) — *Call branch split*: ~1270-line `case NodeType::Call` in `visit()` reduced to a 128-line dispatch ladder. Emission machinery moves to new `codegen/call_dispatch.cpp`: `emit_builtin_call`, `emit_stereo_native_call`, `emit_chord_expanded_call`, `emit_generic_builtin_tail`. Shared `BuiltinEmitCtx` struct. Also fixes `CEDAR_HOST_EXTENSIONS` config broken by Phase 4a.

- **Phase 5** (`9e23178d`) — *seqpat_transform consolidation*: 5 `SequenceProgram`-recompiling transform handlers (bank/anchor/variant/mode/tune) collapse to one `emit_seqpat_transform` with a `SeqpatTransformHooks` config struct. Combined LOC reduction ~520 → ~213. All 18 pattern-transform builtins tagged `kind=PatternTransform`; unit test guards the tags.

- **Phase 6** (`6416bf29`) — *Data-driven viz + param emitters*: 5 visualization handlers (419 LOC) collapse to one `emit_visualization` driven by `VizMeta`; 4 param-control handlers (363 LOC) collapse to one `emit_param` driven by `ParamMeta`. `viz.cpp` + `params.cpp`: 782 → 370 LOC. `ParamType` + `VisualizationType` move to `builtin_info.hpp`; 9 entries tagged `kind=Visualization/Param`. Unit test guards the metadata.

- **Phase 7** (`6aa66c53`) — *Close-out*: `apply_lambda` / `apply_function_ref` / `inline_mixer_closure` hot paths gain `RAII NodeTypesFrame` (zero per-iteration bucket allocation). Large monolith functions split: `emit_bus_epilogue` 249 → 37 LOC, `handle_field_access` 192 → 66, `handle_record_literal` 112 → 45, `emit_event_transform` 366 → 58 (7 stage helpers). No function in `codegen/` exceeds ~125 LOC. PRD status DONE.

---

## web/

**1 commit — 4 files**

- **hardening Phase 8** (`26f92c9f`) — shape index requests moved from AudioWorklet to compile worker. `compile.worker.ts` handles `get-shape-index` messages; `wasm-backend.ts` routes to the worker; `cedar-processor.js` drops the `getShapeIndex` handler (~70 lines removed). `nkido_wasm.cpp` updated to new `akkado_get_shape_index()` / `serialize_shape_index()` signatures.

---

## docs/

**5 commits — ~12 file-changes**

- `docs/prd-parser-codegen-hardening.md` — phases 6–9 shipped; PRD status DONE.
- `docs/prd-codegen-sprawl-cleanup.md` — phases 1a–7 shipped; PRD status DONE.
- `docs/parallelism-blocker-catalogue.md` — open hazards reduced to one (PB-F5); PB-002/003/004 moved to out-of-scope with owners; "Precondition list for PRD-3" section added.
- `docs/audit/parser-codegen-interop_audit_2026-05-25.md` — resolution tallies and RESOLVED tags added across phases 6–9 and sprawl-cleanup phases 4–7.
