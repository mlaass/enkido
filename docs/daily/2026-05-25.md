# Daily Summary 2026-05-25

**Commits:** 17 (excluding merge and bot-authored summary)  
**Contributors:** Moritz Laass

---

## Highlights

A highly productive day centred on completing **Phase 5 of the Runtime Event Transforms PRD** — chord-array read/write in `event_map` closures, the scales catalog, key/scale stdlib dispatchers, and the `voice`/`invert` stdlib functions. Alongside that, two architectural bugs were fixed in the Cedar VM and Akkado codegen, and param-type annotations reached Phase 2.

---

## akkado/

**17 files touched** across `src/`, `stdlib/`, `tests/`, and `include/`.

- **feat(akkado): param-type annotations Phase 2** (`1b7b317`) — Ships seven annotation keywords (`evs`, `sig`, `num`, `rec`, `arr`, `str`, `fn`) on user function parameters. Lexer, parser, `ParamValueType`, and codegen updated; five new variants for `Number`, `DynArray`, `Record`, `String`, `Function`. Also fixes a pre-existing `parse_prefix` infinite-loop bug (unhandled token no longer stalls the allocator). Renames 13 test files that used the new reserved words as identifiers. 192 assertions across 30 cases; docs rebuilt. (26 files, ~923 insertions)
- **fix(akkado-codegen): canonical closure-body recovery handles bare-identifier bodies** (`e457b7c`) — Six closure-body walker sites in codegen were using type-based child detection, silently consuming a bare-identifier body (e.g. `(h) -> h`) as a phantom parameter. Adds `codegen::closure_body(arena, closure_node)` helper and migrates all sites. Regression test upgraded to catch the silent `BUFFER_UNUSED` failure. (6 files, 120 ins / 155 del)
- **refactor(typed_value): Phase 5 Commit I — collapse EventSource into Pattern** (`00a8d34`) — `ValueType::EventSource` and `EventSourcePayload` were entirely dead code; no call site ever produced them. Removed struct, factory, enum value, and 8 dead check branches across codegen. (8 files, 45 ins / 78 del)
- **feat(event-transforms): Phase 5 Commit G — stdlib `voice` + `invert`** (`d137fc3`) — `voice(events, intervals)` synthesises a chord from a single-note event; `invert(events, axis)` reflects chord voices around an axis MIDI pitch. Also fixes a latent bug: `Pitch`-atom events left `e.notes[0]` at zero — now set to `e.midi_note`. (3 files, 96 ins)
- **feat(event-transforms): Phase 5 Commit F — stdlib `key` + `scale`** (`8c19539`) — Scale-quantise dispatchers (12 roots × 8 scale types) and pitch-class key quantisation shipped as generated Akkado stdlib. Generator: `web/scripts/generate-scale-quantize.ts`. Includes 9-case test file `test_scale_quantize.cpp`. (5 files, 1317 ins)
- **refactor(stdlib): `scale_quantize` uses shared `snap`/`pc12` helper fns** (`405be8f`) — Now that `execute_step` dispatches `BLOCK_CALL` inside `run_event_map_closure`, the 96-branch catalog can factor shared rounding logic into reusable `fn snap` / `fn pc12` helpers instead of inlining. File shrinks 108 KB → 94 KB (~13%). (2 files, 628 ins / 642 del)
- **feat(event-transforms): Phase 5 Commit C — `scales.ak` dispatcher catalog + generator** (`14db122`) — `web/scripts/generate-scales.ts` emits 39 scales + aliases from tonal.js subset into `akkado/stdlib/scales.ak` with twin dispatchers `scale_intervals(name)` and `scale_length(name)`. (4 files, 459 ins)
- **feat(event-transforms): Phase 5 Commit B — wire `scales.ak` into stdlib bundle** (`13de2a5`) — Placeholder file added; CMake glob auto-picks it up. Two smoke tests confirm wiring end-to-end. (2 files, 39 ins)

---

## cedar/

**4 files touched** in `include/cedar/vm/`, `include/cedar/opcodes/`, and `src/vm/`.

- **fix(cedar-vm): unify subprogram-body dispatch via `execute_step`** (`d95aa8b`) — Critical architectural fix: meta-opcodes (`BLOCK_CALL`, `BLOCK_BIND`, `SKIP_IF_*`, `LOOP_STATIC`) were only dispatched inside `execute_program`'s main loop. Every other subprogram-body runner drove instructions through `VM::execute()`, which had no case for them — causing silent no-ops. Extracts `VM::execute_step(slot, body, ip)` helper and migrates all six body-runner loops. Three new `[event-map][block_call][subprogram-dispatch]` tests. (3 files, 235 ins / 75 del)
- **feat(event-transforms): Phase 5 Commit E — chord-array WRITE in `event_map` closures** (`258cb51`) — Widens `EVENT_OUT_COUNT` 7→10 with `NOTES_DATA`, `FREQS_DATA`, `NUM_VALUES` slots; write mask widened to 16 bits. Runtime overlay: notes-only → derive freqs via mtof; freqs-only → derive notes via ftom; both → verbatim copy; stale tail zeroed. 7 test cases. (5 files, 370 ins / 22 del)
- **feat(event-transforms): Phase 5 Commit D — chord-array READ in `event_map` closures** (`7ff8ae6`) — Exposes `e.notes` and `e.freqs` as `DynArray` fields in `build_event_record`. New enum naming 10 input-bank slots; `fill_event_record_bank` packs `evt.notes[]`/`evt.values[]` and zeros tail. 4 test cases. (4 files, 165 ins / 23 del)

---

## docs/

**5 files touched** (all PRDs).

- **docs(prd): add memory integrity tests PRD** (`b97161a`) — New `docs/prd-memory-integrity-tests.md` (789 lines): four-harness plan covering explosion guard, ASan/LSan/UBSan CI, zero-alloc trap, and mutating-recompile drift fuzz. Motivated by a 48 GB RSS explosion in `akkado-cli --check`. (1 file, 789 ins)
- **docs(prd): expand pattern-array-transforms to design-ready** (`f6cc5fd`) — Fleshes the 110-line draft into a full PRD: 3-phase plan (substrate → stdlib lowering → cutover), renames clock primitive to `event_speed`, adds `event_random` / `pattern_active` / `pattern_event` primitives. Resolves Q1–Q6. (1 file, 522 ins / 50 del)
- **docs(prd): record Phase 5 followup commits** (`2a8ee1f`) — Documents closure-walker fix (e457b7c), `execute_step` unification (d95aa8b), and `scale_quantize` backfill (405be8f) against their originating PRDs. (2 files, 23 ins)
- **docs(prd): note Phase 5 EventSource→Pattern collapse in param-type-annotations Phase 2** (`3e6bac4`) — Adds caveat to PRD §1.1/§3.1/§4.1/§4.2 that `ValueType::EventSource` is now collapsed into `PatternPayload` (pre-implementation reconciliation). (1 file, 29 ins)
- **docs(event-transforms): file `degrade` + `mask` todo** (`2e7ea90`) — Phase 5 Commit H investigated and deferred; dependency on `random()` per-event scalar and `pattern_active_at_time` filed as Q6 on `prd-pattern-array-transforms.md`. (1 file, 1 ins)

---

## scripts/ / web/

**3 files touched** in `scripts/` and `web/scripts/`.

- **scripts(list-prds): show line count column per PRD** (`fb7ec9c`) — `scripts/list-prds.py` gains a `lines` column for at-a-glance PRD size. (1 file, 21 ins / 6 del)
- `web/scripts/generate-scale-quantize.ts` — Updated by both the Phase 5 Commit F (initial generation, 359 ins) and the `snap`/`pc12` refactor (Commit F+refactor passes).
- `web/scripts/generate-scales.ts` — New generator for `scales.ak` (299 ins), added alongside `web/package.json` tonal.js dependency.
