// Akkado stdlib — scale catalog
//
// Placeholder file shipping a single no-op smoke fn. The real catalog
// (a dispatcher fn `scale_intervals(name)` covering every tonal.js scale
// type) lands in Phase 5 Commit C, generated by
// `web/scripts/generate-scales.ts`.
//
// This file exists so Commit B can verify that a fresh stdlib `.ak`
// file is picked up by the CMake glob + loader plumbing without a
// rebuild of the cmake configure step.
//
// Tracker: docs/prd-runtime-event-transforms.md §9 Phase 5.

fn scales_smoke() -> { 0 }
